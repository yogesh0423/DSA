#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    
    if (n % 400 == 0) {
        cout << "Leap year";
    }
    else if (n % 100 == 0) {
        cout << "Not a leap year";
    }
    else if (n % 4 == 0) {
        cout << "Leap year";
    }
    else {
        cout << "Not a leap year";
    }
    return 0;
}
